# ── Domain ───────────────────────────────────────────────────────────────────
DOMAIN=localhost
AUTH_DOMAIN=auth.localhost

# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=appdb
POSTGRES_USER=appuser
POSTGRES_PASSWORD=changeme

# ── MinIO ─────────────────────────────────────────────────────────────────────
MINIO_HOST=minio
MINIO_PORT=9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme
MINIO_BUCKET=uploads
MINIO_PRESIGNED_EXPIRY=3600
# Browser-reachable base for presigned URLs (nginx proxies /storage -> minio:9000)
MINIO_PUBLIC_URL=http://localhost/storage

# ── Keycloak ──────────────────────────────────────────────────────────────────
KEYCLOAK_HOST=keycloak
KEYCLOAK_PORT=8080
KEYCLOAK_DB=keycloak
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme
KEYCLOAK_REALM=app
KEYCLOAK_CLIENT_ID=app-frontend
KEYCLOAK_CLIENT_SECRET=changeme

# ── Backend ───────────────────────────────────────────────────────────────────
SECRET_KEY=changeme-generate-with-openssl-rand-hex-32
LOG_LEVEL=INFO
CORS_ORIGINS=["http://localhost","http://localhost:5173"]

# ── Redis / Queue ────────────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0

# ── Devin API (course generation) — v3 org-scoped API ────────────────────────
# Service-user token from Devin (Settings > Service users). Starts with "cog_".
DEVIN_API_KEY=
# Your Devin organization id (Settings > Service users, or GET /v3/self -> org_id).
DEVIN_ORG_ID=
DEVIN_API_BASE_URL=https://api.devin.ai/v3
# Optional snapshot/playbook to make generation deterministic & fast.
DEVIN_SNAPSHOT_ID=
DEVIN_PLAYBOOK_ID=
DEVIN_MAX_ACU_LIMIT=20

# ── Media generation for interactive courses (all optional) ──────────────────
# Pixverse (https://platform.pixverse.ai) — images + short video clips.
# Requires account credits; with no key/credits the course uses SVG placeholders.
PIXVERSE_API_KEY=
PIXVERSE_API_BASE_URL=https://app-api.pixverse.ai
PIXVERSE_IMAGE_MODEL=gpt-image-2.0
PIXVERSE_IMAGE_PATH=/openapi/v2/image/generation
PIXVERSE_VIDEO_MODEL=v4.5
# Google Cloud Text-to-Speech (REST, API-key auth) — narration audio.
GOOGLE_TTS_API_KEY=
GOOGLE_TTS_VOICE=en-US-Studio-O
GOOGLE_TTS_LANGUAGE=en-US
# Per-course caps to bound latency/cost.
MEDIA_MAX_IMAGES=10
MEDIA_MAX_VIDEOS=2
MEDIA_MAX_AUDIO=16
MEDIA_POLL_TIMEOUT=240

# ── Platform / Multi-tenant ──────────────────────────────────────────────────
DEMO_COMPANY_SLUG=acme
COURSES_BUCKET=courses
PLATFORM_PUBLIC_URL=http://localhost
